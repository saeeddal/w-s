export interface IAppSetting {
  // maxAppWidth: number;
  header: IHeader;
  appVersion: string;
  callCenter: string;
}

export interface IHeader {
  backgroundColor: string;
  headerHeight: number;
}
